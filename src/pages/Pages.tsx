import React from 'react'

import stylePosition from '../styles/generics/position.module.scss'

const Pages: React.FC = () => {
    return (
        <div>
            <h1 className={stylePosition['text-center']}>Hello, React!</h1>
        </div>
    )
}

export default Pages
